#include <iostream>

using namespace std;

int main()
{
    /*int hei;
    cout << "Enter your height(cm):";
    cin >> hei;

    if (hei < 150 ){

        cout << "You are dwarf.";
    }else if(hei >= 165 && hei <= 195){

        cout << "You are tall.";
    }else if( hei <= 150 && hei >= 165)  {

    cout << "You are average.";
    }else {

    cout << "you are abnormal.";
    }
    */
    /*
    int a, b, c;
    cout << "Enter three number:";
    cin >> a >> b >> c;

    if (a > b && a > c){

        cout << a << " is the larest number.";
    }
    else if (a < b && a < c){
        cout << b << " is the largest number.";
    } else {
        cout << c << " is the largest number.";
    }
    */
    /*

    int x, y;

    cout << "Enter the value for X and y coordinate:";
    cin >> x >> y;

    if (x > 0 && y > 0){

        cout << "The coordinate point  lies " << x << "," << y << " in the First quandrant.";
    }else if (x > 0 && y < 0){

        cout << "The coordinate point  lies " << x << "," << y << " in the second quandrant.";
    }else if (x < 0 && y < 0){

        cout << "The coordinate point  lies " << x << "," << y << " in the third quandrant.";
    }else if (x < 0 && y > 0){

        cout << "The coordinate point  lies " << x << "," << y << " in the forth quandrant.";
    }
    */

    /*
    int markP, markC, markM;
    cout << "Input the marks obtained in Physics:";
    cin >> markP;
    cout << "Input the marks obtained in chemistry:";
    cin >> markC;
    cout << "Input the marks obtained in Mathematics:";
    cin >> markM;

    int markT;
    markT = (markP + markC + markM);
    cout << "Total marks of Math, Physics and Chemistry:"<< markT << endl;

    int markMP;
    markMP = (markP + markM);
    cout << "Total marks of Maths and Physics:" << markMP << endl;

    if (markM >= 65){
            if (markP >= 55)
                if (markC >= 50)
            if (markT >= 190)
    if (markMP >= 140)

    cout << "The candidate is eligible.\n";
    }
    else {
        cout << "The candidate is not eligible.";
    }

    */

    int num;
    cout << "Enter a number:";
    cin >> num;

    if (x=-b +- root b^2 -4ac / 2a)
        if


    return 0;
}
